package org.example;


import weka.core.Instances;

public class Main {
    /*
    public static void main(String[] args) {
        try {
            // Cargar el archivo XMLBIF
            BIFLoader loader = new BIFLoader();
            loader.setSource(new File("ruta_del_archivo.xml"));
            Instances instances = loader.getDataSet();

            // Establecer el atributo de clase (si es necesario)
            int classIndex = instances.numAttributes() - 1;
            instances.setClassIndex(classIndex);

            // Aquí puedes realizar cualquier procesamiento adicional con las instancias

            // Imprimir las probabilidades posteriores
            int numInstances = instances.numInstances();
            for (int i = 0; i < numInstances; i++) {
                double[] posteriorProbabilities = /* Calcular las probabilidades posteriores */;
          /*      System.out.println("Probabilidades posteriores para la instancia " + i + ": ");
                for (int j = 0; j < posteriorProbabilities.length; j++) {
                    System.out.println("Clase " + j + ": " + posteriorProbabilities[j]);
                }
                System.out.println();
            }
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
    */
}
